// import { gl } from "./context";

// // console.log("Shader xD");

// // const vertexShaderSource = `
// // #version 300 es
 
// // // an attribute is an input (in) to a vertex shader.
// // // It will receive data from a buffer
// // in vec4 a_position;
 
// // // all shaders have a main function
// // void main() {
// //   gl_Position = a_position;
// // }
// // `;

// // const fragmentShaderSource = `
// // #version 300 es
// // out vec4 outColor;
 
// // void main() {
// //   // Nice color
// //   outColor = vec3(0, 0.5, 0);
// // }
// // `;

// // let fragment = createShader(gl.FRAGMENT_SHADER, fragmentShaderSource);
// // let vertex = createShader(gl.VERTEX_SHADER, vertexShaderSource);

// // function createShader(type, src) {
// //   var shader = gl.createShader(type);
// //   gl.shaderSource(shader, src);
// //   gl.compileShader(shader);

// //   var success = gl.getShaderParameter(shader, gl.COMPILE_STATUS);
// //   if (success) {
// //       console.log("Shader compilation is ok 👌😂💯💯");
// //     return shader;
// //   }
// // }
